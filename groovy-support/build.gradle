plugins {
    id 'groovy'
}

repositories {
    maven { url = 'https://repo.jenkins-ci.org/public/' }
}

dependencies {
    compile "org.jenkins-ci.main:jenkins-core:$jenkins_core_version"
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
